import { ChangeLookDirective } from './change-look.directive';

describe('ChangeLookDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeLookDirective();
    expect(directive).toBeTruthy();
  });
});
